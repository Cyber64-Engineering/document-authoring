indices:
  cyber-bank:
    include:
      - /financial-site/accounts/**
    exclude:
      - /financial-site/confidential/**
    target: /financial-site/data/search-data/query-index.json
    properties:
      title:
        select: title
        value: innerHTML(el)
      image:
        selectFirst: img 
        value: attribute(el, "src")
      description:
        select: meta[name="description"]
        value: attribute(el, "content")