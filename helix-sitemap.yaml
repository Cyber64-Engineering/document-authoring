sitemaps:
   aik:
     lastmod: YYYY-MM-DD
     languages:
       rs:
         source: /financial-site/query-index.json
         destination: /sitemap-rs.xml
         hreflang: rs
       en:
         source: /en/query-index.json
         destination: /sitemap-en.xml
         hreflang: en
         alternate: /en/{path}